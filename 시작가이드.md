# Peace Blog Backend - 빠른 시작 가이드

## 🚀 5분 만에 시작하기

### 1단계: Java 확인

터미널에서 실행:
```bash
java -version
```

Java 17 이상이 설치되어 있어야 합니다.
없다면 https://adoptium.net 에서 다운로드하세요.

### 2단계: 프로젝트 실행

**Mac/Linux:**
```bash
cd peace-blog-backend
chmod +x gradlew
./gradlew bootRun
```

**Windows:**
```cmd
cd peace-blog-backend
gradlew.bat bootRun
```

### 3단계: 서버 확인

브라우저에서 열기:
```
http://localhost:8080/api/posts
```

초기 Mock 게시글 3개가 JSON으로 표시되면 성공! 🎉

## 📱 테스트해보기

### 1. 게시글 목록 조회
```bash
curl http://localhost:8080/api/posts
```

### 2. 새 게시글 작성
```bash
curl -X POST http://localhost:8080/api/posts \
  -H "Content-Type: application/json" \
  -d '{
    "title": "내 첫 게시글",
    "content": "<p>안녕하세요!</p>",
    "author": "DJ"
  }'
```

### 3. 특정 게시글 조회
```bash
curl http://localhost:8080/api/posts/1
```

## 🔗 React 프론트엔드 연결

프론트엔드 `src/services/api.js` 파일에서:

```javascript
const API_BASE_URL = 'http://localhost:8080/api';
```

이미 설정되어 있으므로 그대로 사용하면 됩니다!

## ⚙️ 설정 변경

### 포트 변경
`src/main/resources/application.yml`:
```yaml
server:
  port: 9090  # 원하는 포트로 변경
```

### CORS 출처 추가
```yaml
cors:
  allowed-origins:
    - http://localhost:3000
    - https://your-domain.com  # 추가
```

## 🛠 IDE에서 실행

### IntelliJ IDEA
1. File > Open > build.gradle 선택
2. Gradle 임포트 대기
3. PeaceBlogApplication 우클릭 > Run

### VS Code
1. Extension Pack for Java 설치
2. 프로젝트 폴더 열기
3. Run > Start Debugging (F5)

### Eclipse
1. File > Import > Gradle > Existing Gradle Project
2. 프로젝트 선택
3. Run As > Spring Boot App

## 📊 로그 확인

애플리케이션이 시작되면 다음과 같은 로그가 표시됩니다:

```
=================================================
Peace Blog Backend is ready!
Server running on: http://localhost:8080
API Base Path: http://localhost:8080/api
=================================================
Available Endpoints:
  GET    /api/posts           - 모든 게시글 조회
  GET    /api/posts/{id}      - 특정 게시글 조회
  POST   /api/posts           - 게시글 작성
  PUT    /api/posts/{id}      - 게시글 수정
  DELETE /api/posts/{id}      - 게시글 삭제
  POST   /api/images/upload   - 이미지 업로드
=================================================
```

## 🐛 문제 해결

### "java: command not found"
→ Java가 설치되지 않았습니다. https://adoptium.net 에서 설치

### "Permission denied: ./gradlew"
→ 실행 권한 부여:
```bash
chmod +x gradlew
```

### 포트 충돌 (8080 already in use)
→ application.yml에서 포트 변경 또는:
```bash
./gradlew bootRun --args='--server.port=9090'
```

### CORS 에러
→ application.yml의 `cors.allowed-origins`에 프론트엔드 주소 추가

## 📦 JAR 파일로 배포

```bash
# 빌드
./gradlew build

# 실행
java -jar build/libs/peace-blog-backend-0.0.1-SNAPSHOT.jar
```

## 🔄 개발 중 Hot Reload

Gradle을 사용한 자동 재시작:
```bash
./gradlew bootRun --continuous
```

## 📖 더 알아보기

- `README.md` - 전체 문서
- `/src/main/java/` - 소스 코드 (주석 참고)
- 로그 레벨 변경: application.yml의 `logging.level` 수정

## 💡 다음 단계

1. ✅ 백엔드 실행 완료
2. ✅ API 테스트 완료
3. 🔜 React 프론트엔드 연결
4. 🔜 실제 데이터베이스 연동 (PostgreSQL)

화이팅! 🚀
